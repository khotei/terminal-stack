{
  "name": "terminal-stack",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bun-run TypeScript for terminal-stack's scripts. The scripts RUN with zero install (bun reads .ts directly); the deps here are dev-only — editor types + tsc.",
  "scripts": {
    "doctor": "bun scripts/doctor.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
