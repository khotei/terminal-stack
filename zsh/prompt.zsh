# Prompt — Starship (config: ~/.config/starship.toml, Catppuccin Mocha palette).
# Sourced last so it initialises over a fully-configured shell.
command -v starship >/dev/null && eval "$(starship init zsh)"
