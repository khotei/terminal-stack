// Zellij default layout — the everyday session bar (zjstatus).
// Loaded for every new session via `default_layout "default"` in config.kdl.
// Needs the plugin wasm at ~/.config/zellij/plugins/zjstatus.wasm (install.sh
// fetches it, like autolock). If it's missing the bar pane shows an error but
// the session still runs — the always-valid fallback is `zellij -l compact`
// (the built-in bar). Bar config mirrors layouts/dev.kdl — keep the two in sync.
// Source: github.com/dj95/zjstatus · palette: Catppuccin Mocha (see config.kdl)
layout {
    pane size=1 borderless=true {
        plugin location="file:~/.config/zellij/plugins/zjstatus.wasm" {
            format_left   "{mode}#[bg=#181825] #[fg=#89b4fa,bold]{session}"
            format_center "{tabs}"
            format_right  "{command_git_branch}#[bg=#181825,fg=#6c7086]{datetime}"
            format_space  "#[bg=#181825]"

            mode_normal          "#[bg=#a6e3a1,fg=#181825,bold] NORMAL "
            mode_locked          "#[bg=#585b70,fg=#cdd6f4,bold] LOCKED "
            mode_default_to_mode "normal"

            tab_normal "#[bg=#181825,fg=#6c7086] {index} {name} "
            tab_active "#[bg=#a6e3a1,fg=#181825,bold] {index} {name} "

            command_git_branch_command    "git branch --show-current"
            command_git_branch_format     "#[bg=#181825,fg=#f5c2e7]  {stdout} "
            command_git_branch_interval   "3"
            command_git_branch_rendermode "static"

            datetime          "#[bg=#181825,fg=#6c7086] {format} "
            datetime_format   "%H:%M"
            datetime_timezone "Europe/Kiev"
        }
    }
    pane
}
